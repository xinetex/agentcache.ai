.wizard-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  animation: fadeIn 0.3s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.wizard-modal {
  width: 90%;
  max-width: 700px;
  background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
  border-radius: 1.5rem;
  border: 1px solid #475569;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.8);
  display: flex;
  flex-direction: column;
  max-height: 85vh;
  animation: slideUp 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(20px) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.wizard-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1.5rem;
  border-bottom: 1px solid #334155;
}

.wizard-header h2 {
  font-size: 1.5rem;
  font-weight: 600;
  color  colorf9;
}

.close-btn {
  background: none;
  border: none;
  color: #94a3b8;
  font-size: 1.5rem;
  cursor: pointer;
  padding: 0.25rem;
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 0.375rem;
  transition: all 0.2s;
}

.close-btn:hover {
  background: #33  background: #33 f5f9;
  transform: rotate(90deg);
}

.wizard-body {
  flex: 1;
  pa  pa  pa  pa  pa  pa  p-y: auto;
}

.wizard-step h3 {
  font-size: 1.125rem;
  font-weight: 600;
  color: #f1f5f9;
  margin-bottom: 1.5rem;
}

.use-case-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1rem  gap: 1rem  gap: 11.5rem;
}



gap: 1rem  g {gap: 1rem  g {gap: 1rem  g {gap: 1rem  g {gadient(135deg, #0f172a 0%, #020617 100%);
  border: 2px solid #334155;
  bo  bo  bo  bo  bem;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relativ  position: relativ  position: rele-car  position: relatien  position: relativabsolute;
  top: 0;
                  h: 10           ht: 100px;
  backgroun  backgroun  bient(circle, rgba(14, 165, 233, 0.1) 0%,   backgroun  back
  opacity: 0;
  transition: opacity   transitiose-ca  transition: o
  border-color: #0ea5e9;
  transform: translateY(-4  transform: translat8px 24px rgba(14, 165, 233, 0.2);
}

.use-case-card:.use-case-card:.use-case-card:.use-casease-card.selected {
  border-color: #0ea5e9;
  background:   background:   background:   backg5, 233, 0.15) 0%, rgba(  background:   backg00  background:   ba0 0 0 1px rgba(14, 165, 233, 0.3), 0 8px 24px rgba(14, 165, 233, 0.3);
}

.use-case-card.selected::before {
  opacity: 1;
}

.use-case-card h4 {
  font-size: 1rem;
  font-weight: 600;
  color: #f1f5f9;
  margin-bottom: 0.5rem;
}

.use-case-card p {
  font-size: 0.875rem;
  color: #94a3b8;
}

.custom-prompt {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.custom-prompt label {
  font-size: 0.875rem;
  font-weight: 500;
  color: #e2e8f0;
}

.custom-prompt textarea {
  padding: 0.75rem;
  background: #0f172a;
  border: 1px solid #334155;
  border-radius: 0.5rem;
  color: #e2e8f0;
  font-size: 0.875rem;
  font-family: inherit;
  resize: vertical;
  transition: all 0.3s;
}

.custom-prompt textarea:focus {
  outline: none;
  border-color: #0ea5e9;
  box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.1);
}

.performance-options {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.perf-option {
  display: flex;
  gap: 1rem;
  padding: 1.25rem;
  background: linear-gradient(135deg, #0f172a 0%, #020617 100%);
  border: 2px solid #334155;
  border-radius: 1rem;
  cursor: poin  cursor: poin  cursor: poin  cuc-  cursor: po0, 0.2, 1);
}

.perf-option:hover {
  border  border  border   transform: translat  border  border  borde0 4px 16px rgba(14, 165,  border  border  boroption.selected {
  bor  bor  bor  bor5e9;
  background: linear-gradient(135deg, rg  background: 3, 0.  background: linear- 2  background: linear-x-shadow: 0 0 0 1px rgba(14, 165, 233, 0.3);
}

.perf-icon {
  font-size: 2rem;
}

.perf-info {
  flex: 1;
  dis  dis  dis  dis  dis  dis  n:  dis  dis  dis  dis  d;
}

.perf-info h4 {
  font-size: 1rem;
  font-weight: 600;
  color  color  color  cof-info p {
  font-size: 0.875rem;
  color: #94a3b8;
}

.perf-.perf-.perf-.pe-size: 0.75rem;
  color: #10b981;
  font-weight: 600;
}

.loading-state,
.error-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: cen  justify-cing: 3rem;
  text-align: center;
}

.spinner {
  width: 48px;
  height: 48px;
  border: 4px solid #33415  border: 4px solid #33415 5e  border: 4px solid #0%  border: 4px solid #33415  border: 4px solid #33415 5e  border: 4px  trans  border: 4px solid); }
}

.loading-state p {
  margin-top: 1.5rem;
  font-size: 1.125rem;
  font-weight: 500;
  color: #f1f5f9;
}

.loading-subtitle {
                         color: #94a3b8;
  margin-top: 0.5rem;
}

.error-message,
.error-state p {
  color: #ef4444;
  font-size: 0.875rem;
  margin-top: 1rem;
}

.wizard-footer {
  display:  display:  displcontent: flex-end;  display.75rem;
  padding: 1.5rem;
  border-top: 1px solid #334155;
}
